## Packages
framer-motion | Page transitions and interactive animations for ADHD-friendly feedback
canvas-confetti | For winning streaks and positive reinforcement
@types/canvas-confetti | TypeScript definitions for canvas-confetti

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

Space theme aesthetic with dark mode default.
Using user ID 1 as the default user for demonstration purposes (since auth is not specified).
